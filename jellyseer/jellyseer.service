[Unit]
Description=Jellyseer
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker compose -f /home/gart/jellyseer/docker-compose.yml up
ExecStop=/usr/bin/docker compose -f /home/gart/jellyseer/docker-compose.yml down

[Install]
WantedBy=default.target
